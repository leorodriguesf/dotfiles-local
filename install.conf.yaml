- defaults:
    link:
      create: true
      relink: true

- clean: ["~", "~/.config"]

- link:
    ~/.shell_local_before: shell_local_before
    ~/.shell_local_after: shell_local_after
    ~/.zshrc_local_before: zshrc_local_before
    ~/.zshrc_local_after: zshrc_local_after
    ~/.bashrc_local_before: bashrc_local_before
    ~/.bashrc_local_after: bashrc_local_after

    ~/.config/aerospace: aerospace
    ~/.hammerspoon: hammerspoon
    ~/.config/hammerspoon: hammerspoon

- shell:
    - [git submodule update --init --recursive, Installing submodules]

    - command: |
        ansible-playbook ansible/secrets.yaml
      description: Setting up your secret tools
      stdin: true
      stdout: true
      stderr: true

    - command: |
        ansible-playbook ansible/ssh.yaml --ask-vault-pass
      description: Installing ssh keys
      stdin: true
      stdout: true
      stderr: true

    - command: |
        ansible-playbook ansible/base.yaml ansible/workstation.yaml ansible/ABI.yaml --ask-vault-pass --ask-become-pass
      description: Installing base tools and workstation
      stdin: true
      stdout: true
      stderr: true
